# Shared Packages

Reusable components may live here after they are required by at least two applications.

Likely candidates:

- Shared TypeScript types
- Design tokens and UI primitives
- Authentication interfaces
- Audit-event schemas
- Content metadata
- Common validation utilities

Application-specific logic should remain inside its owning application.
