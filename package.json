{
  "name": "good-steward",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "cyber:scan": "npm run scan --workspace @good-steward/cyber-agents"
  },
  "engines": { "node": ">=20" }
}
